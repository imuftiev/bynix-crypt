version: '3.3'
services:
  db:
    image: postgres
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_PASSWORD: ${POSTGRES_DB}
    ports:
      - {POSTGRES_PORT}